# Session Management

NextClaw UI provides a session operations panel (**Sessions** tab) with full management capabilities:

- List sessions with search and channel filtering
- Inspect session chat history
- Edit per-session metadata (`label`, `preferredModel`)
- Clear session history
- Delete sessions

This is useful when running multi-agent routing and channel operations long term — you can clean or retarget problematic sessions without hand-editing files.
