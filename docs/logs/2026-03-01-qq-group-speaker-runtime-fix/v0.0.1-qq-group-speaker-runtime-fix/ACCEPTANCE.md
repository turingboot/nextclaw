# Acceptance

## 用户/产品视角验收步骤

1. 保持 QQ 群聊会话策略为“同群共享一个 session”（不按 user 拆分）。
2. 在同一个群里让两位成员分别发送不同内容。
3. 检查运行日志/调试输出中的入模文本，确认每条群消息都带：
   - `speaker:user_id=<对应发言者>`
   - （可选）`name=<昵称>`
4. 连续追问“刚才谁提到发版/谁提到测试”，确认 AI 能按不同发言者回答。
5. 再发送一条私聊消息，确认不强制加群聊 speaker 前缀，不影响私聊体验。
